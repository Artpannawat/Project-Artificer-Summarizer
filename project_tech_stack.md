
# 🛠️ Tech Stack & Library Usage - Artificer Summarizer Project
(สำหรับนำเสนอ/Presentation Reference)

เอกสารนี้สรุปเทคโนโลยีและไลบรารีที่ใช้ในการพัฒนา โดยเน้นส่วนประมวลผลข้อความ (Text Processing) เป็นหลัก เพื่อตอบคำถามเกี่ยวกับสถาปัตยกรรมของระบบ

---

## 🏗️ 1. Core Architecture (ภาพรวมระบบ)
โปรเจกต์นี้ใช้สถาปัตยกรรมแบบ **Modern Web Application** แยกส่วน Frontend/Backend ชัดเจน:

*   **Frontend (หน้าบ้าน):** Vue.js 3 (Vite) + CSS Glassmorphism
*   **Backend (หลังบ้าน):** Python (FastAPI) - เน้นความเร็วและการประมวลผล AI
*   **Database (ฐานข้อมูล):** MongoDB (NoSQL) - เก็บข้อมูล User และ History แบบยืดหยุ่น
*   **Deployment:** Vercel (Serverless Environment)

---

## 📚 2. Key Libraries & Modules (พระเอกของงาน)

### 🧠 A. ส่วนประมวลผลและสรุปความ (Summarization Engine)
ระบบนี้ใช้ **Hybrid Engine** ทำงานขนานกัน 2 ระบบ:

#### 1. Artificer AI Engine (Intelligence Mode)
*   **Technology:** **Google Gemini API** (`google-generativeai`)
*   **File:** `backend/app/main.py` (ฟังก์ชัน `summarize_with_ai`)
*   **หน้าที่:** ส่งข้อความไปยังโมเดล LLM (Gemini 2.0 Flash/2.5) เพื่อให้ AI "อ่านและเขียนสรุปใหม่" (Abstractive Summarization) ให้เป็นภาษามนุษย์ที่สละสลวย
*   **จุดเด่น:** เข้าใจบริบท, แปลภาษาได้, สรุปได้เนียนเหมือนคนเขียน

#### 2. Basic Engine (Traditional Mode)
*   **Technology:** **TextRank Algorithm** (Graph-Based) + **Regex**
*   **File:** `backend/app/summarizer/summarization_model.py`
*   **หน้าที่:** ใช้คณิตศาสตร์คำนวณ "น้ำหนัก" ของประโยคสำคัญ โดยไม่ใช้ AI ภายนอก
    *   **Graph Construction:** สร้างกราฟความสัมพันธ์ระหว่างประโยค
    *   **Jaccard Similarity:** คำนวณความเหมือนของคำในแต่ละประโยค
    *   **Ranking:** เลือกประโยคที่มีคะแนนสูงสุดมาเรียงต่อกัน
*   **จุดเด่น:** ทำงานได้แม้ไม่มีเน็ต, เร็วมาก, ไม่เสียค่า API, เหมาะสำหรับสรุปใจความสำคัญแบบตรงไปตรงมา

### 📝 B. ส่วนประมวลผลข้อความ (Text Preprocessing)
ก่อนส่งเข้า Engine ข้อความดิบจะถูกทำความสะอาดที่นี่:
*   **File:** `backend/app/summarizer/text_processor.py`
*   **Libraries:** `re` (Regular Expressions - Built-in Python)
*   **หน้าที่:**
    *   **Cleaning:** ลบตัวอักษรขยะ, Space แปลกๆ, จัดระเบียบย่อหน้า
    *   **Segmentation (สำคัญ):** **ตัดประโยคภาษาไทย** โดยใช้ Heuristics (กฎเกณฑ์) ที่เขียนขึ้นเอง เพราะภาษาไทยไม่มีจุด Full stop เหมือนภาษาอังกฤษ เราจึงต้องเขียน Logic เพื่อตรวจจับ "การเว้นวรรค" และ "คำเชื่อม" เพื่อแยกประโยคให้ถูกต้อง

### 📂 C. ส่วนจัดการไฟล์ (File Processing)
รองรับการอัปโหลดไฟล์ PDF และ Word:
*   **File:** `backend/app/summarizer/file_processor.py`
*   **PDF:** ใช้ library **`PyPDF2`** (เลือกใช้ตัวนี้แทน PyMuPDF เพราะขนาดเล็กกว่า เหมาะกับ Serverless ที่มีพื้นที่จำกัด)
*   **DOCX:** ใช้ library **`python-docx`** และ **`docx2txt`** ดึงข้อความออกมา
*   **Images:** ใช้ library **`Pillow`** (PIL) สำหรับย่อรูปโปรไฟล์ก่อนเก็บลง Database

---

## 🚀 3. สรุปไฟล์สำคัญ (File Structure Map)

| ไฟล์ / โฟลเดอร์ | หน้าที่หลัก |
| :--- | :--- |
| **`backend/app/main.py`** | **The Brain:** ศูนย์กลางควบคุม API ทั้งหมด, เชื่อมต่อ Gemini AI, และจัดการ Flow การทำงานขนาน (Parallel Execution) |
| **`summarizer/text_processor.py`** | **The Cleaner:** แม่บ้านทำความสะอาดข้อความ ล้างขยะ และหั่นประโยคให้พร้อมใช้งาน |
| **`summarizer/summarization_model.py`** | **The Mathematician:** สมองฝั่ง Basic คำนวณกราฟประโยค (TextRank) |
| **`summarizer/file_processor.py`** | **The Translator:** ล่ามแปลไฟล์ PDF/Word ให้เป็นตัวหนังสือธรรมดา |
| **`routers/users.py`** | **Profile Manager:** จัดการเรื่อง User, Register, Login, และ Avatar (แปลงรูปเป็น Base64) |
| **`auth/auth_handler.py`** | **The Guard:** ดูแลความปลอดภัย, ตรวจสอบ Password (Bcrypt/SHA256), ออกบัตรผ่าน (JWT Token) |

---

## 💡 Q&A Suggestions (แนวคำตอบเมื่อโดนถาม)

**Q: ทำไมใช้ Python ทำ Backend?**
**A:** เพราะ Python มี Library สนับสนุนด้าน AI และ Data Processing ที่แข็งแกร่งที่สุด ทำให้จัดการกับ Text และเชื่อมต่อกับ Gemini ได้ง่ายและเสถียร

**Q: ทำไม Basic Engine ถึงสรุปได้โดยไม่ต้องใช้ AI?**
**A:** เราใช้อัลกอริทึม **TextRank** ซึ่งใช้หลักการเดียวกับ Google PageRank แต่เปลี่ยนจาก "เว็บที่มีลิงก์โยงหากันเยอะคือเว็บดัง" เป็น "ประโยคที่มีคำซ้ำกับประโยคอื่นเยอะคือใจความสำคัญ"

**Q: เจอปัญหาอะไรและแก้ยังไง?**
**A:** ปัญหาเรื่อง **Deployment Size** บน Vercel (จำกัด 250MB) เราแก้โดยการเปลี่ยน Library อ่าน PDF จากตัวใหญ่ (PyMuPDF) มาเป็นตัวเล็ก (PyPDF2) และเขียนระบบตัดคำภาษาไทยเองแทนการใช้ Library สำเร็จรูปขนาดใหญ่ (เช่น PyThaiNLP) ทำให้ระบบเบาและเร็ว
