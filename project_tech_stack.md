
# 🛠️ Tech Stack & Library Usage - Artificer Summarizer Project
(สำหรับนำเสนอ/Presentation Reference)

เอกสารนี้สรุปเทคโนโลยีและไลบรารีที่ใช้ในการพัฒนา โดยเน้นส่วนประมวลผลข้อความ (Text Processing) เป็นหลัก เพื่อตอบคำถามเกี่ยวกับสถาปัตยกรรมของระบบ

---

## 🏗️ 1. Core Architecture (ภาพรวมระบบ)
โปรเจกต์นี้ใช้สถาปัตยกรรมแบบ **Modern Web Application** แยกส่วน Frontend/Backend ชัดเจน:

*   **Frontend (หน้าบ้าน):** Vue.js 3 (Vite) + CSS Glassmorphism
*   **Backend (หลังบ้าน):** Python (FastAPI) - เน้นความเร็วและการประมวลผล AI
*   **Database (ฐานข้อมูล):** MongoDB (NoSQL) - เก็บข้อมูล User และ History แบบยืดหยุ่น
*   **Deployment:** Vercel (Serverless Environment)

---

## 📚 2. Key Libraries & Modules (พระเอกของงาน)

### 🧠 A. ส่วนประมวลผลและสรุปความ (Summarization Engine)
ระบบนี้ใช้ **Hybrid Engine** ทำงานขนานกัน 2 ระบบ:

#### 1. Artificer AI Engine (Intelligence Mode)
*   **Technology:** **Google Gemini API** (`google-generativeai`)
*   **File:** `backend/app/main.py`
*   **Technique:** **Structured Prompt Engineering**
    *   ใช้คำสั่งพิเศษ (System Prompt) บังคับให้ AI สรุปเป็น "Bullet Points" เท่านั้น
    *   กำหนดตรรกะ (Logic) ชัดเจน: 1 ข้อ = Main Idea, 2 ข้อ = ปัญหา+ทางแก้, 3+ ข้อ = เล่าเรื่องเป็นฉากๆ
*   **จุดเด่น:** เข้าใจบริบทลึกซึ้ง (Context Awareness), แปลภาษาได้, สรุปเป็นภาษามนุษย์ที่อ่านง่าย

#### 2. Basic Engine (Traditional Mode)
*   **Technology:** **TextRank Algorithm** (Graph-Based) + **Custom Thai Tokenizer**
*   **File:** `backend/app/summarizer/summarization_model.py`
*   **Technique:** **Extractive Summarization without NLP Libraries**
    *   **Custom Tokenizer:** เขียนระบบตัดคำไทยเองโดยใช้ **MaxMatch Algorithm** และ Dictionary ฝังในโค้ด (ตัดปัญหาเรื่อง Library PyThaiNLP ใหญ่เกินไป)
    *   **TextRank:** ใช้กราฟคณิตศาสตร์คำนวณ "น้ำหนัก" ของประโยคที่มีความเชื่อมโยงกันมากที่สุด
    *   **Formatter:** จัดรูปแบบประโยคที่คัดมาแล้วให้เป็น Bullet Points เหมือน AI
*   **จุดเด่น:** ทำงานได้แม้ไม่มีเน็ต, เร็วมาก, ไม่เสียค่า API, ขนาดเล็กมาก (Lightweight)

### 📝 B. ส่วนประมวลผลข้อความ (Text Preprocessing)
*   **File:** `backend/app/summarizer/text_processor.py`
*   **Libraries:** `re` (Regular Expressions)
*   **หน้าที่:**
    *   **Cleaning:** ลบตัวอักษรขยะ, Space แปลกๆ
    *   **Validation:** ตรวจสอบความถูกต้องของอีเมลขั้นสูง (DNS Check) โดยใช้ `email-validator` เพื่อดูว่าโดเมน @gmail.com นั้นมีอยู่จริงหรือไม่

### 📂 C. ส่วนจัดการไฟล์ (File Processing)
*   **PD:** ใช้ library **`PyPDF2`** (เลือกใช้ตัวนี้แทน PyMuPDF เพราะขนาดเล็กกว่า 10 เท่า เหมาะกับ Serverless)
*   **DOCX:** ใช้ library **`python-docx`**
*   **Images:** ใช้ library **`Pillow`** (PIL) สำหรับย่อรูปโปรไฟล์เป็น Base64

---

## 🚀 3. สรุปไฟล์สำคัญ (File Structure Map)

| ไฟล์ / โฟลเดอร์ | หน้าที่หลัก |
| :--- | :--- |
| **`backend/app/main.py`** | **The Brain:** ควบคุม API, สั่งงาน Gemini AI ด้วย Prompt พิเศษ, และตรวจสอบอีเมล (Email Validation) |
| **`summarizer/text_processor.py`** | **The Cleaner & Cutter:** ตัดคำไทยด้วย MaxMatch Algorithm และทำความสะอาดข้อความ |
| **`summarizer/summarization_model.py`** | **The Mathematician:** คำนวณกราฟ TextRank เพื่อหาประโยคสำคัญโดยไม่ต้องพึ่ง AI |
| **`summarizer/file_processor.py`** | **The Translator:** แปลงไฟล์ PDF/Word เป็นข้อความดิบ |
| **`routers/users.py`** | **Profile Manager:** จัดการรูปโปรไฟล์ (Avatar) แบบ Base64 |

---

## 💡 Q&A Suggestions (แนวคำตอบเมื่อโดนถาม)

**Q: ทำไม Basic Engine ถึงอ่านภาษาไทยรู้เรื่องทั้งที่ไม่ใช้ Library?**
**A:** เพราะเราเขียน **"Custom Thai Tokenizer"** ขึ้นมาเองครับ ใช้เทคนิค Dictionary-based Matching เพื่อตัดคำไทย ทำให้เราไม่ต้องลง Library ขนาดใหญ่อย่าง PyThaiNLP (100MB+) ช่วยลดขนาดแอปเหลือไม่ถึง 50MB ลงบน Vercel ได้สบายๆ

**Q: AI Engine กับ Basic Engine ต่างกันยังไง?**
**A:** **AI** คือการ "อ่านแล้วเขียนใหม่" (เรียบเรียงใหม่หมด) ส่วน **Basic** คือการ "คัดมาแปะ" (เลือกประโยคที่สำคัญที่สุดจากต้นฉบับมาเลย) ข้อดีของ Basic คือไม่มีทางมั่ว (Hallucination) เพราะเอามาจากต้นฉบับเป๊ะๆ

**Q: ระบบตรวจสอบอีเมลเชื่อถือได้แค่ไหน?**
**A:** เราใช้การตรวจสอบถึงระดับ **DNS Records (MX Check)** ครับ คือเช็คไปที่ Server ของ Gmail จริงๆ ว่าโดเมนนี้รับอีเมลได้ไหม ไม่ใช่แค่ดูว่ามีเครื่องหมาย @ หรือไม่ ทำให้กันอีเมลปลอมได้ดีมาก
