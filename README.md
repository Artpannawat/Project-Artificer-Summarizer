# Artificer Summarizer - เครื่องมือการสรุปบทความ

เครื่องมือสรุปบทความอัจฉริยะที่รองรับทั้งภาษาไทยและภาษาอังกฤษ

## ✨ คุณสมบัติ

- 🤖 **AI-powered summarization** - ใช้เทคนิค TF-IDF สำหรับการสรุปบทความ
- 🇹🇭 **รองรับภาษาไทย** - ประมวลผลภาษาไทยได้อย่างแม่นยำ
- 🇺🇸 **รองรับภาษาอังกฤษ** - ประมวลผลภาษาอังกฤษได้อย่างมีประสิทธิภาพ
- 📁 **รองรับไฟล์เอกสาร** - อัปโหลดไฟล์ .txt และ .docx ได้โดยตรง
- 🌐 **Web Interface** - ใช้งานง่ายผ่านเว็บเบราว์เซอร์
- 🔐 **ระบบ Authentication** - ลงทะเบียนและเข้าสู่ระบบ
- 📊 **ปรับแต่งได้** - เลือกจำนวนประโยคที่ต้องการสรุป

## 🚀 การติดตั้ง

### ข้อกำหนดเบื้องต้น
- Python 3.8+
- Node.js 16+
- MongoDB

### Backend Setup

1. **Clone repository**
```bash
git clone <repository-url>
cd Artificer-Summarizer
```

2. **ติดตั้ง Python dependencies**
```bash
cd backend
python -m venv venv
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate

pip install -r requirements.txt
```

3. **ตั้งค่า Environment Variables**
สร้างไฟล์ `.env` ในโฟลเดอร์ `backend`:
```env
MONGODB_URL=mongodb://localhost:27017
JWT_SECRET=your-secret-key
```

4. **รัน Backend Server**
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### Frontend Setup

1. **ติดตั้ง Node.js dependencies**
```bash
cd frontend
npm install
```

2. **รัน Frontend Server**
```bash
npm run dev
```

## 📖 การใช้งาน

### วิธีการที่ 1: อัปโหลดไฟล์
1. เปิดเบราว์เซอร์ไปที่ `http://localhost:5173`
2. ลงทะเบียนหรือเข้าสู่ระบบ
3. กดปุ่ม "เลือกไฟล์ (.txt, .docx)" ในส่วน "อัปโหลดไฟล์"
4. เลือกไฟล์ที่ต้องการสรุป (รองรับ .txt และ .docx)
5. เลือกจำนวนประโยคที่ต้องการสรุป
6. กดปุ่ม "สรุปจากไฟล์"
7. ดูผลลัพธ์การสรุป

### วิธีการที่ 2: ใส่ข้อความโดยตรง
1. เปิดเบราว์เซอร์ไปที่ `http://localhost:5173`
2. ลงทะเบียนหรือเข้าสู่ระบบ
3. ใส่ข้อความที่ต้องการสรุปในช่อง "หรือใส่ข้อความโดยตรง"
4. เลือกจำนวนประโยคที่ต้องการสรุป
5. กดปุ่ม "สรุป"
6. ดูผลลัพธ์การสรุป

## 🔧 การพัฒนา

### โครงสร้างโปรเจค
```
Artificer-Summarizer/
├── backend/
│   ├── app/
│   │   ├── main.py              # FastAPI application
│   │   ├── auth/                # Authentication modules
│   │   ├── database/            # Database configuration
│   │   ├── models/              # Data models
│   │   └── summarizer/          # Summarization logic
│   └── requirements.txt         # Python dependencies
├── frontend/
│   ├── src/
│   │   ├── components/          # Vue.js components
│   │   └── main.js             # Vue.js entry point
│   └── package.json            # Node.js dependencies
└── README.md
```

### API Endpoints

- `POST /register` - ลงทะเบียนผู้ใช้
- `POST /login` - เข้าสู่ระบบ
- `POST /summarize` - สรุปบทความจากข้อความ
- `POST /summarize-file` - สรุปบทความจากไฟล์ (.txt, .docx)
- `GET /health` - ตรวจสอบสถานะเซิร์ฟเวอร์

## 🛠️ เทคโนโลยีที่ใช้

### Backend
- **FastAPI** - Web framework
- **PyThaiNLP** - Thai language processing
- **scikit-learn** - Machine learning for summarization
- **python-docx** - DOCX file processing
- **python-multipart** - File upload handling
- **MongoDB** - Database
- **JWT** - Authentication

### Frontend
- **Vue.js 3** - Frontend framework
- **Vite** - Build tool
- **Axios** - HTTP client

## 🐛 การแก้ไขปัญหา

### ปัญหาที่พบบ่อย

1. **"No module named 'pycrfsuite'"**
   ```bash
   pip install python-crfsuite
   ```

2. **เซิร์ฟเวอร์ไม่เริ่ม**
   - ตรวจสอบว่า MongoDB ทำงานอยู่
   - ตรวจสอบ environment variables

3. **Frontend ไม่เชื่อมต่อกับ Backend**
   - ตรวจสอบ URL ใน `Summarizer.vue`
   - ตรวจสอบ CORS settings

4. **ไฟล์อัปโหลดไม่ทำงาน**
   - ตรวจสอบว่า `python-multipart` ติดตั้งแล้ว
   - ตรวจสอบขนาดไฟล์ (สูงสุด 10MB)
   - ตรวจสอบประเภทไฟล์ (รองรับ .txt และ .docx เท่านั้น)

## 📝 License

MIT License

## 🤝 Contributing

1. Fork the repository
2. Create a feature branch
3. Commit your changes
4. Push to the branch
5. Create a Pull Request

## 📞 Support

หากมีปัญหาหรือคำถาม กรุณาสร้าง issue ใน GitHub repository
