{
    "name": "artificer-monorepo",
    "version": "1.0.0",
    "scripts": {
        "build": "cd frontend && npm install && npm run build"
    },
    "devDependencies": {
        "vercel": "latest"
    }
}